# Required for admin UI login
ADMIN_KEY=change-me

# Optional: required for Notion snapshot / publish commands
NOTION_TOKEN=
NOTION_DATABASE_ID=

# Admin server defaults
HOST=127.0.0.1
PORT=8765

# Set to 1 only when you intentionally want a remote tunnel
ALLOW_REMOTE_TUNNEL=0
